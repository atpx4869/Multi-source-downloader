# Web应用识别逻辑详解

## 📋 整体流程

```
用户上传Excel
    ↓
Web应用接收文件 (web_app.py)
    ↓
解析配置参数 (列号、开始行等)
    ↓
后台线程处理Excel
    ↓
逐行提取标准号
    ↓
StandardProcessor 处理标准号
    ↓
返回结果：完整标准号 + 名称
    ↓
写入Excel并下载
```

---

## 🔍 核心识别逻辑 - 两条路线

### 路线1️⃣：标准号【不带年代号】

**示例输入**：`GB/T 3324` 或 `GB/T 3324` 或 `GB T 3324`

```
输入：GB/T 3324
  ↓
【第1步】标准化格式
  → 去除首尾空格
  → 统一空格：`GB/T 3324` 或 `GB T 3324` → `GB/T 3324`
  ↓
【第2步】判断是否带年代号
  → 使用正则：^([A-Z/]+\s*\d+)(?:-(\d{4}))?$
  → 检测 `-2024` 这样的年代号
  → 结果：不带年代号 ✓
  ↓
【第3步】搜索现行标准
  → 并行搜索3个源（GBW、BY、ZBY）
  → 收集所有搜索结果
  ↓
【第4步】筛选现行标准
  方案A（优先）：查找标记为"现行"的标准
    → 筛选 status 中包含 "现行" 的结果
    → 如果多个，选择年份最新的
    
  方案B（备选）：未找到标记为"现行"时
    → 从所有结果中提取带年份的标准
    → 按年份倒序排序
    → 选择最新的版本
    
  方案C（最后）：完全未找到
    → 返回错误："未找到现行标准"
  ↓
【第5步】返回结果
  ✅ 成功时：(完整标准号, 标准名称, "成功")
  ❌ 失败时：(原始标准号, "", "查询失败: 原因")

示例结果：
  输入：GB/T 3324
  输出：(GB/T 3324-2024, "测试方法标准名称", "成功")
```

---

### 路线2️⃣：标准号【带年代号】

**示例输入**：`GB/T 3324-2024` 或 `GB T 3324-2024`

```
输入：GB/T 3324-2024
  ↓
【第1步】标准化格式
  → 去除首尾空格
  → 统一空格：`GB/T 3324-2024` → `GB/T 3324-2024`
  ↓
【第2步】判断是否带年代号
  → 使用正则：^([A-Z/]+\s*\d+)(?:-(\d{4}))?$
  → 检测到 `-2024` 的年代号
  → 结果：带年代号 ✓
  ↓
【第3步】直接查询标准名称
  → 并行搜索3个源（GBW、BY、ZBY）
  → 收集所有搜索结果
  ↓
【第4步】精确匹配
  方案A（优先）：查找完全匹配的标准
    → 比较 std_no == "GB/T 3324-2024"（忽略空格）
    → 返回对应的标准名称
    
  方案B（备选）：未找到精确匹配
    → 返回第一个搜索结果（可能是同标准不同年份）
    → 带上警告提示："未找到精确匹配，返回最接近的结果"
    
  方案C（最后）：完全未找到
    → 返回错误："未找到标准"
  ↓
【第5步】返回结果
  ✅ 成功时：(原始标准号, 标准名称, "成功")
  ❌ 失败时：(原始标准号, "", "查询失败: 原因")

示例结果：
  输入：GB/T 3324-2024
  输出：(GB/T 3324-2024, "测试方法标准名称", "成功")
```

---

## 📝 标准号格式识别规则

### 正则表达式
```python
pattern = r'^([A-Z/]+\s*\d+)(?:-(\d{4}))?$'
```

### 匹配规则详解

| 模式 | 说明 | 是否有效 |
|------|------|--------|
| `GB/T 3324` | 基础格式（空格） | ✅ |
| `GB/T3324` | 基础格式（无空格） | ✅ |
| `GB T 3324` | 变异格式（空格替换斜杠） | ✅ |
| `GB/T 3324-2024` | 完整格式（带年代号） | ✅ |
| `GB/T3324-2024` | 完整格式（无空格） | ✅ |
| `GB 3324` | 单字母前缀（单个标准） | ✅ |
| `ISO 9001` | 国际标准 | ✅ |
| `ISO 9001-2015` | 国际标准带年代 | ✅ |
| `GB/T` | 不完整（无编号） | ❌ |
| `3324` | 只有编号 | ❌ |
| `UNKNOWN 999` | 无效前缀 | ❌ |
| `` | 空值 | ❌ |

---

## 🔄 搜索流程详解

### 步骤1：并行搜索三个源

```python
results = self.router.search_all(std_no, limit=50)
# 返回格式：
# {
#     SourceType.GBW: SearchResponse(...),
#     SourceType.BY: SearchResponse(...),
#     SourceType.ZBY: SearchResponse(...)
# }
```

每个源独立搜索，不受其他源影响，支持：
- **GBW** (国家标准平台)：HTTP API搜索
- **BY** (标院内网)：需要登录的系统
- **ZBY** (正规宝)：Playwright自动化浏览器

### 步骤2：收集结果

```python
all_standards = []
for source_type, response in results.items():
    if response.error:
        # 记录错误但继续
        print(f"⚠️  {source_type.value} 搜索失败: {response.error}")
        continue
    
    if response.count > 0:
        print(f"✓ {source_type.value} 找到 {response.count} 个结果")
        # 合并所有源的结果
        all_standards.extend(response.standards)
```

**特点**：
- 即使某个源失败，仍继续用其他源的结果
- 支持多源结果汇聚（去重由应用逻辑处理）

### 步骤3：现行标准优先筛选

```python
# 优先找"现行"标准
current_standards = [
    std for std in all_standards 
    if std.status and '现行' in std.status
]

if current_standards:
    # 如果多个现行标准，选最新的
    year_standards.sort(key=lambda x: x[1], reverse=True)
    return year_standards[0]
else:
    # 备选：没有"现行"标记，选最新年份的
    return 按年份倒序的第一个标准
```

**优先级**：
1. `status` 字段包含"现行"
2. 同为现行时，年份最新的优先
3. 都不满足时，返回任何版本的最新年份

---

## 🎯 Excel处理工作流

### 路由 1: `/upload` (POST)

**功能**：上传和初始化处理

```python
def upload_file():
    # 1. 验证文件
    if not allowed_file(file.filename):  # 只支持 .xlsx, .xls
        return error
    
    # 2. 保存文件
    filepath = UPLOAD_FOLDER / f"{task_id}_{filename}"
    
    # 3. 创建任务对象
    task = ProcessTask(task_id)
    tasks[task_id] = task
    
    # 4. 启动后台线程
    thread = threading.Thread(
        target=process_excel_task,
        args=(task_id, filepath, std_no_col, ...)
    )
    thread.daemon = True
    thread.start()
    
    return {'task_id': task_id, 'message': '开始处理'}
```

### 路由 2: `/status/<task_id>` (GET)

**功能**：实时查询处理进度

```python
def get_status(task_id):
    task = tasks.get(task_id)
    return {
        'status': 'processing',      # pending, processing, completed, failed
        'progress': 50,              # 0-100
        'total': 100,
        'current_row': 50,
        'success_count': 48,
        'fail_count': 2,
        'logs': [最近10条日志],
        'elapsed_time': 12.5
    }
```

### 路由 3: `/download/<task_id>` (GET)

**功能**：下载结果文件

```python
def download_result(task_id):
    # 生成下载文件名
    download_name = f"标准号处理结果_20260109_091412.xlsx"
    return send_file(result_path, download_name)
```

### 后台处理线程

```python
def process_excel_task(task_id, input_file, std_no_col, start_row, ...):
    task.status = 'processing'
    
    # 读取Excel
    df = pd.read_excel(input_file)
    
    # 列号转换：A → 0, B → 1, ...
    col_idx = ord(std_no_col.upper()) - ord('A')
    
    # 逐行处理
    for idx in range(start_row - 1, len(df)):
        std_no = df.iloc[idx, col_idx]
        
        if pd.isna(std_no) or std_no.strip() == '':
            task.add_log(f"行 {idx+1}: 空值，跳过")
            continue
        
        # 调用处理器
        full_std_no, name, status = processor.process_standard(std_no)
        
        # 写入结果
        df.iloc[idx, result_idx_no] = full_std_no
        df.iloc[idx, result_idx_name] = name
        
        # 更新统计
        if "成功" in status:
            task.success_count += 1
        else:
            task.fail_count += 1
        
        task.progress = (idx - start_row + 2) / task.total * 100
        task.add_log(log_message)
    
    # 保存结果
    df.to_excel(result_path, index=False)
    task.status = 'completed'
```

---

## ⚙️ 配置参数详解

### Web表单参数

| 参数 | 类型 | 默认 | 说明 |
|------|------|------|------|
| `file` | File | - | 上传的Excel文件 |
| `std_no_col` | Char | A | 标准号所在列（A/B/C...） |
| `start_row` | Int | 2 | 开始行号（通常第1行是表头） |
| `result_col_no` | Char | B | 结果完整标准号列 |
| `result_col_name` | Char | C | 结果标准名称列 |

### 列号映射规则

```python
# 列号 → 数组索引
col_idx = ord(std_no_col.upper()) - ord('A')

示例：
A → ord('A') - ord('A') = 0
B → ord('B') - ord('A') = 1
C → ord('C') - ord('A') = 2
...
Z → ord('Z') - ord('A') = 25
```

---

## 📊 数据模型

### SearchResponse (来自 api/models.py)

```python
@dataclass
class SearchResponse:
    source: SourceType           # BY/ZBY/GBW
    query: str                   # 搜索词
    count: int                   # 找到的标准数
    standards: List[StandardInfo]  # 结果列表
    error: Optional[str]         # 错误信息
    elapsed_time: float          # 耗时
```

### StandardInfo (来自 api/models.py)

```python
@dataclass
class StandardInfo:
    std_no: str                  # 标准编号：GB/T 3324-2024
    name: str                    # 标准名称
    source: SourceType           # 来源
    has_pdf: bool                # 是否有PDF
    publish_date: Optional[str]  # 发布日期
    implement_date: Optional[str]  # 实施日期
    status: Optional[str]        # 状态：现行/废止/...
    source_meta: Dict            # 源特定元数据
```

---

## 🔄 错误处理

### 异常处理链

```
尝试处理标准号
  ↓
【异常1】文件格式错误
  → 返回："只支持 .xlsx 和 .xls 格式"
  
【异常2】列号超出范围
  → 记录日志并跳过该行
  
【异常3】标准号为空
  → 记录日志并跳过该行
  
【异常4】搜索失败（网络/服务错误）
  → 尝试其他源
  → 如果全部失败，返回："查询失败: 未找到任何结果"
  
【异常5】后台处理线程错误
  → 捕获异常，设置 task.status = 'failed'
  → 记录到 task.error
  → 用户可见错误信息
```

### 重试机制

目前**无自动重试**，但可扩展支持：
- 单个标准号重试（配置次数）
- 源级别重试（源不可用时尝试备用源）

---

## 📈 性能考虑

### 时间复杂度

| 操作 | 耗时 | 备注 |
|------|------|------|
| 读取100行Excel | ~100ms | pandas读取 |
| 单个标准号搜索 | ~5-10s | 3个源并行搜索 |
| 单行处理（含搜索） | ~5-10s | 包括搜索+写入 |
| 100行完整处理 | ~10-15分钟 | 按顺序处理，不支持并行 |

### 改进空间

1. **批量搜索**：改为批量搜索而非逐个
2. **并行处理**：多线程处理多行（当前单线程）
3. **结果缓存**：相同标准号只搜索一次
4. **预加载**：优先加载常用源

---

## 🧪 测试场景

### 测试用例1：完全不带年代号

```
输入列：
A列（标准号）
GB/T 3324

预期结果：
B列（完整标准号）：GB/T 3324-2024
C列（标准名称）：对应名称
日志：✅ 成功
```

### 测试用例2：完整带年代号

```
输入列：
A列（标准号）
GB/T 3324-2024

预期结果：
B列（完整标准号）：GB/T 3324-2024
C列（标准名称）：对应名称
日志：✅ 成功
```

### 测试用例3：混合空格

```
输入列：
A列（标准号）
GB T 3324

预期：自动转换为 GB/T 3324 并搜索
```

### 测试用例4：不存在的标准

```
输入列：
A列（标准号）
GB/T 9999999

预期结果：
B列：GB/T 9999999（原值）
C列：空
日志：❌ 查询失败: 未找到任何结果
```

### 测试用例5：空值行

```
输入列：
A列（标准号）
（空）

预期：
直接跳过，日志：⏭️ 空值，跳过
```

---

## 📌 总结

### 识别逻辑核心

| 识别方式 | 处理流程 | 返回信息 |
|---------|--------|--------|
| **不带年代** | 搜索 → 筛选现行 → 选最新 | 完整标准号 + 名称 |
| **带年代** | 搜索 → 精确匹配 → 返回名称 | 原始标准号 + 名称 |
| **失败** | 多源失败/未找到 | 原始标准号 + 错误信息 |

### 关键特性

✅ 多源并行搜索（GBW/BY/ZBY）  
✅ 智能现行标准选择  
✅ 容错机制（某源失败继续用其他源）  
✅ 实时进度显示  
✅ 灵活列号配置  
✅ 批量处理支持  

---
